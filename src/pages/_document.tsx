import React from 'react';
import {Html, Head, Main, NextScript} from 'next/document';

const Document = () => (
  <Html>
    <Head />
    <body className="bg-slate-200 dark:bg-slate-800 dark:text-white">
      <Main />
      <NextScript />
    </body>
  </Html>
);

export default Document;
